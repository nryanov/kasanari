# Example overlay for OAuth / OIDC auth. Set kasanari.auth.type=oauth (or KASANARI_AUTH_TYPE=oauth) and restart.
kasanari.auth.type=oauth

quarkus.oidc.auth-server-url=http://localhost:8080/realms/kasanari
quarkus.oidc.client-id=kasanari-api
quarkus.oidc.credentials.secret=change-me
